# Catalog Scripts

This directory is reserved for scripts that read `tool.yaml` files and generate or validate catalog views.

Suggested future uses:

- validate required metadata fields
- list all `temporary` or `sunset` tools
- generate Markdown catalog tables
- generate machine-friendly exports for skills or LLM integrations
